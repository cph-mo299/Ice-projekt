import jdk.nashorn.api.scripting.ScriptObjectMirror;

public class CompanySetup {
    CompanySetup<1>

    @Override
    public boolean equals(Object obj) {
        return super.equals(obj);
        getClass(UserLogin)
        for (int i = 0; i < ScriptObjectMirror; i++) {

        }
    }
}
